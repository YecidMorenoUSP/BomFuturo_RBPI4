# Adicionar permisos de ejecución
chmod +x run

# Configurar pyhton
python3 -m venv env
source env/bin/activate

python3 -m pip install paho-mqtt
python3 -m pip install mysql-connector-python
python3 -m pip install pip flask
# python3 -m pip install flask_mysqldb

# Linux
sudo apt-get update
sudo apt install apache2    
sudo apt install php libapache2-mod-php
sudo apt install phpmyadmin
 # apache2 Si
 # PHPMyAdmin with db-common: No
sudo nano /etc/apache2/apache2.conf
 # Include /etc/phpmyadmin/apache.conf
sudo service apache2 restart

# Configurar MySQL
sudo apt install mariadb-server
sudo mysql_secure_installation
 # INTRO Y Y PSSWD Y
 # PSSWD : bomfuturo
sudo mysql -uroot -p
 # CREATE DATABASE bomfuturo;
 # CREATE USER 'bf_db'@'0.0.0.0' IDENTIFIED BY 'bf_pwssd';
 # GRANT ALL PRIVILEGES ON bomfuturo.* TO 'bf_db'@'0.0.0.0';
 # GRANT ALL PRIVILEGES ON *.* TO 'bf_db'@'localhost';
 # DROP USER 'bf_db'@'localhost';
 # FLUSH PRIVILEGES;

sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf


# Configurar MQTT



# Descargar recursos

# Finalizar
